# Workflow Notes

The executable workflow entrypoints for this project are the repository-root `Snakefile` and `run_all.sh`.

This directory is reserved for future workflow configuration, helper profiles, or rule modularization if the project is extended beyond the class submission.
